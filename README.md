
# PKS YOU V DOWNLOADER — v1-pro (Bigger & Production-leaning)
Closest to "no compromise" while staying self-hosted:
- Formats list (video/audio) + file sizes
- Direct + Server proxy
- MP4 merge (2160/1440/1080/720/480)
- MP3 extract
- Subtitles list (download/copy)
- Filename setting, basic rate-limit
- ffmpeg via nixpacks.toml

## Deploy
- Backend → Railway (enable ffmpeg)
- Frontend → Vercel (set BACKEND_BASE)
- Legal: Use only with permission; respect YouTube ToS.
